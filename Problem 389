public class Solution {
    public char FindTheDifference(string s, string t) {
        
        int strS = 0;
        foreach(char c in t){
            strS += c;
        }
        
        foreach(char c in s){
            strS -= c;
        }
        return(char)(strS);
    }
}
